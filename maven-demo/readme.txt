1. pom dependency
    -- default  ->compile ( need during compile and runtime)
    -- provided  only need during compile
    -- runtime   only need during runtime
    -- test      only need during test
    -- system    point to local computer file